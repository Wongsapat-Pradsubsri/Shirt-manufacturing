# 📘 คู่มือฝึกทำ Technical SEO Audit (Astro SSG Playbook)

คู่มือนี้จัดทำขึ้นเพื่อใช้สำหรับเรียนรู้การทำ Technical SEO Audit ในโปรเจกต์ `Shirt-manufacturing` (Astro 6) 
คุณสามารถทำเครื่องหมาย `[x]` ในช่องที่ตรวจเสร็จแล้ว และพิมพ์คำถามหรือจุดที่ไม่เข้าใจลงในส่วน `✍️ คำถาม/ข้อสงสัยของผม` ใต้แต่ละหัวข้อได้เลยครับ

---

## 🛠️ ขั้นตอนที่ 0: เตรียมระบบ Git (แนะนำ)
ก่อนจะเริ่มทำอะไร ให้รันคำสั่งเหล่านี้ใน Terminal ของโปรเจกต์ เพื่อควบคุมเวอร์ชันโค้ด:
```bash
git init
git add .
git commit -m "จุดเริ่มต้นก่อนฝึก Audit"
```
* **ประโยชน์**: ป้องกันโค้ดพัง และทำให้เห็นว่ารุ่นพี่หรือเราปรับปรุงจุดใดไปบ้าง

---

## 🔍 ขั้นตอนที่ 1: ตรวจสอบโครงสร้างไฟล์และโค้ดต้นทาง (Codebase Audit)

### [ ] 1.1 ไฟล์คอนฟิกหลัก: `astro.config.mjs`
* **แนวทางการตรวจ**:
  * เปิดไฟล์ [astro.config.mjs](file:///c:/Users/modem/Desktop/SEO/Shirt-manufacturing/astro.config.mjs)
  * ตรวจเช็กว่า `site: SITE_URL` มีค่าเริ่มต้นตรงกับโดเมนจริงหรือไม่ (ในไฟล์นี้ใช้ punycode `https://xn--o3c1bj3b4bj8cd.com/` เพื่อรองรับโดเมนภาษาไทย "เสื้อแท้.com")
  * ตรวจสอบการกรองของ Sitemap:
    ```javascript
    filter: (page) => !page.endsWith("/blog-previews.json") && !page.includes("/tag/"),
    ```
    *(ทำไมต้องกรอง `/tag/` ออก? ลองหาเหตุผลดูครับ)*
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

### [ ] 1.2 เลย์เอาต์และแกนหลัก SEO: `Layout.astro` และ `SEO.astro`
* **แนวทางการตรวจ**:
  * เปิดไฟล์ [Layout.astro](file:///c:/Users/modem/Desktop/SEO/Shirt-manufacturing/src/layouts/Layout.astro) และ [SEO.astro](file:///c:/Users/modem/Desktop/SEO/Shirt-manufacturing/src/components/SEO.astro)
  * ดูการทำงานใน `<head>` ของ `Layout.astro` ว่ามีแท็กบอร์ดแคสต์ที่สำคัญต่อมือถือตัวนี้หรือไม่:
    ```html
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    ```
  * ดูใน `SEO.astro` ว่ามีตัวแปรสร้าง Canonical URL อย่างไร:
    ```javascript
    const canonicalURL = new URL(Astro.url.pathname, Astro.site);
    ```
    *(ทำไม canonical URL ถึงสำคัญ และทำไมห้ามใช้ localhost ค้างไว้ใน production?)*
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

### [ ] 1.3 เนื้อหาและ Metadata ใน Markdown/MDX
* **แนวทางการตรวจ**:
  * เข้าไปดูไฟล์ใน `src/content/blog/` และ `src/content/products/`
  * ตรวจเช็กหัวไฟล์ (Frontmatter) ว่ามีการกำหนดค่าเหล่านี้เหมาะสมหรือไม่:
    - `title`: ความยาวพอดี (ไม่เกิน 60 ตัวอักษร) และมี keyword ขึ้นต้น
    - `description`: สรุปใจความบทความ (ความยาวประมาณ 120-155 ตัวอักษร)
    - `imageAlt`: เขียนอธิบายรูปภาพ เช่น `"เสื้อยืดสีขาวแบรนด์ธนพลัส"` แทนคำสั้น ๆ เช่น `"image"`
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

## 🌐 ขั้นตอนที่ 2: ตรวจสอบหน้าเว็บสด (Local Live Server Audit)
ให้คุณพิมพ์คำสั่ง `npm run dev` ใน Terminal แล้วเปิดหน้าเว็บ `http://localhost:4321`

### [ ] 2.1 โครงสร้าง Heading แท็ก `<h1>` ถึง `<h6>`
* **แนวทางการตรวจ**:
  * เปิดเว็บหน้าหลัก หน้าบริการ หรือสินค้า จากนั้นกด F12 -> แท็บ Elements
  * ค้นหาแท็ก `<h1>` (กด Ctrl+F แล้วพิมพ์ `h1` ในกล่องค้นหา)
  * เช็กว่า **หน้าเว็บแต่ละหน้ามี `<h1>` เพียงหน้าละ 1 ตัวเท่านั้น** หรือไม่?
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

### [ ] 2.2 การแสดงผลบนอุปกรณ์มือถือ (Mobile Usability)
* **แนวทางการตรวจ**:
  * เปิด F12 ในหน้าเว็บ จากนั้นกดปุ่มจำลองขนาดหน้าจอมือถือ (Device Toolbar) ด้านบนซ้าย
  * ลองสไลด์ดูหน้าเว็บในรูปแบบสมาร์ทโฟน
  * มีปุ่มไหนที่กดได้ยาก (ขนาดเล็กกว่า 44x44px) หรือมีเนื้อหาล้นหน้าจอจนเกิดการเลื่อนแนวนอน (Horizontal Scrolling) หรือไม่?
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

### [ ] 2.3 ตรวจสอบ Schema Markup (JSON-LD)
* **แนวทางการตรวจ**:
  * กดดู Source Code ของหน้าเว็บ (`Ctrl + U`)
  * มองหาบล็อก `<script type="application/ld+json">`
  * ก็อปปี้โครงสร้าง JSON ด้านในไปวางใน [Google Rich Results Test](https://search.google.com/test/rich-results) (ถ้าเน็ตใช้งานได้) หรือตรวจสอบว่ามี Error ในโครงสร้างหรือไม่
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

## 📦 ขั้นตอนที่ 3: ตรวจสอบไฟล์ผลลัพธ์หลังจากทำการ Build (Production Build Audit)
รันคำสั่งด้านล่างใน Terminal เพื่อสร้างโฟลเดอร์รวบรวมไฟล์ปลายทางสำหรับนำขึ้น Server:
```bash
npm run build
```
ระบบจะสร้างโฟลเดอร์ชื่อ `dist/` ขึ้นมา ให้ตรวจสอบไฟล์ภายในโฟลเดอร์นี้:

### [ ] 3.1 การตรวจสอบไฟล์ `dist/robots.txt` และ Sitemap XML
* **แนวทางการตรวจ**:
  * เปิดดูไฟล์ `dist/robots.txt` ว่ามีโครงสร้างแบบนี้หรือไม่:
    ```text
    User-agent: *
    Allow: /
    Disallow: /*?
    Sitemap: https://xn--o3c1bj3b4bj8cd.com/sitemap-index.xml
    Sitemap: https://xn--o3c1bj3b4bj8cd.com/sitemap-0.xml
    ```
  * เปิดไฟล์ `dist/sitemap-0.xml` ดูว่ามี URL ของหน้าสำคัญครบถ้วนหรือไม่ และ**หน้าที่มีสถานะ noindex (ไม่ต้องการให้ Google ค้นพบ) ต้องไม่มีอยู่ใน sitemap นี้**
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

### [ ] 3.2 ความสม่ำเสมอของ URL และปัญหา Trailing Slash
* **แนวทางการตรวจ**:
  * เปิดไฟล์ HTML ในโฟลเดอร์ `dist/` เช่น `dist/services/index.html`
  * ตรวจสอบว่า URL ภายในแท็ก canonical และลิงก์นำทางในเว็บไซต์มีเครื่องหมาย `/` ลงท้าย (Trailing Slash) เหมือนกันทั้งหมดหรือไม่ (เช่น `/services/` เทียบกับ `/services` ที่ไม่มีสแลช)
  * *เพราะอะไรถึงต้องมีให้เหมือนกัน?*
* **✍️ คำถาม/ข้อสงสัยของผมในข้อนี้**:
  * *พิมพ์ตรงนี้...*

---

## 🎓 วิธีการคุยกันต่อจากนี้:
1. คุณสามารถเปิดไฟล์นี้ใน VS Code แนะนำให้เปิดอ่านและทำเครื่องหมาย `[x]` เมื่อตรวจเสร็จ
2. หากคุณสงสัยในหัวข้อไหน พิมพ์คำถามไว้ในช่อง `✍️ คำถาม/ข้อสงสัยของผมในข้อนี้`
3. จากนั้นส่งข้อความมาหาผมในแชทนี้ได้เลยครับว่าอยากให้ช่วยอธิบายหัวข้อไหนเพิ่ม!
